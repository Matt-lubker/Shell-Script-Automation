This script will:

    Update the package list.
    Upgrade all installed packages.
    Perform a full upgrade (e.g., installing new dependencies or replacing obsolete packages).
    Clean up unused packages and cached files to save disk space.




1) Make the script executable:
    chmod +x update_upgrade.sh

2) Run the script:
    ./update_upgrade.sh
